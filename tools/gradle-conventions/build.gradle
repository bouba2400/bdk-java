plugins {
    id 'groovy-gradle-plugin'
}

allprojects {
    apply plugin: 'groovy-gradle-plugin'

    repositories {
        gradlePluginPortal()
    }

    tasks.register('fix') {
        group = 'verification'
    }
}

subprojects {
}
