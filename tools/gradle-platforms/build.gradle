plugins {
    id 'java-platform'
}

allprojects {
    apply plugin: 'java-platform'

    group = 'net.bdk'

    javaPlatform {
        allowDependencies()
    }

    tasks.register('fix') {
        group = 'verification'
    }
}

subprojects {
}
